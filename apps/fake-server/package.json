{
  "name": "fake-server",
  "version": "1.0.0",
  "description": "fake server for the mfe's api calls",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --port 5000",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Marcos Iglesias",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
